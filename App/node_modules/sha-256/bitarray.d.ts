declare const _default: () => void;
export default _default;
/**
 * Make a partial word for a bit array.
 * @param {Number} len The number of bits in the word.
 * @param {Number} x The bits.
 * @param {Number} [_end=0] Pass 1 if x has already been shifted to the high side.
 * @return {Number} The partial word.
 */
export declare function partial(len: number, x: number, end?: number): number;
/**
 * Concatenate two bit arrays.
 * @param {bitArray} a1 The first array.
 * @param {bitArray} a2 The second array.
 * @return {bitArray} The concatenation of a1 and a2.
 */
export declare function concat(a1: number[], a2: number[]): number[];
/**
 * Find the length of an array of bits.
 * @param {bitArray} a The array.
 * @return {Number} The length of a, in bits.
 */
export declare function bitLength(a: number[]): number;
/**
 * Truncate an array.
 * @param {bitArray} a The array.
 * @param {Number} len The length to truncate to, in bits.
 * @return {bitArray} A new array, truncated to len bits.
 */
export declare function clamp(a: number[], len: number): number[];
