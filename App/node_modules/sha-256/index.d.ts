export declare function hash(str: string | number[]): string;
export declare function hmac(key: string | number[], data: string | number[]): string;
export declare class SHA256 {
    private key;
    private init;
    private h;
    private buffer;
    private length;
    constructor(hash?: SHA256);
    reset(): this;
    update(data: string | number[]): this;
    finalize(): number[];
    private precompute();
    /**
   * Perform one cycle of SHA-256.
   * @param {Uint32Array|bitArray} w one block of words.
   * @private
   */
    private block(w);
}
